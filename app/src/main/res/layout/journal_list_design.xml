<?xml version="1.0" encoding="utf-8"?>
<androidx.cardview.widget.CardView xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    android:layout_width="match_parent"
    android:layout_height="100dp"
    android:layout_margin="10dp"
    app:cardCornerRadius="5dp"
    app:cardElevation="8dp">

    <RelativeLayout
        android:layout_width="match_parent"
        android:layout_height="match_parent"
        android:background="@color/card3"
        android:elevation="8dp"
        android:padding="15dp">

        <TextView
            android:id="@+id/journal_card_title"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:fontFamily="@font/greycliff_bold"
            android:text="@string/journal_card_title_placeholder"
            android:textSize="20sp" />

        <TextView
            android:id="@+id/journal_card_summary"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:layout_below="@+id/journal_card_title"
            android:layout_marginTop="25dp"
            android:fontFamily="@font/greycliff_demibold"
            android:text="@string/journal_card_summary_placeholder"
            android:textSize="15sp" />

        <ImageView
            android:id="@+id/journal_card_image"
            android:layout_width="100dp"
            android:layout_height="100dp"
            android:layout_alignParentEnd="true"
            android:layout_alignParentBottom="true"
            android:contentDescription="@string/logo_desc"
            android:src="@drawable/logo_color" />
    </RelativeLayout>

</androidx.cardview.widget.CardView>